import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(
  document.getElementById('react-sample') as HTMLDivElement
).render(
  <p>This line is rendered by <code>/assets/react/sample.tsx</code></p>
)
